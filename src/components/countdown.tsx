"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export function Countdown({ target, serverNow, frozen=false }: { target: string; serverNow: string; frozen?: boolean }) { const router=useRouter(); const offset=Date.parse(serverNow)-Date.now(); const [remaining,setRemaining]=useState(()=>Math.max(0,Date.parse(target)-(Date.now()+offset))); useEffect(()=>{const refresh=()=>router.refresh();window.addEventListener("focus",refresh);window.addEventListener("online",refresh);if(frozen)return()=>{window.removeEventListener("focus",refresh);window.removeEventListener("online",refresh)};const id=setInterval(()=>{const value=Math.max(0,Date.parse(target)-(Date.now()+offset)); setRemaining(value); if(value===0){clearInterval(id);router.refresh();}},1000);return()=>{clearInterval(id);window.removeEventListener("focus",refresh);window.removeEventListener("online",refresh)}},[target,offset,router,frozen]); const seconds=Math.floor(remaining/1000); const days=Math.floor(seconds/86400); const hours=Math.floor((seconds%86400)/3600); const minutes=Math.floor((seconds%3600)/60); return <strong className="countdown">{days}d {String(hours).padStart(2,'0')}h {String(minutes).padStart(2,'0')}m {String(seconds%60).padStart(2,'0')}s</strong>; }
