export type CommandOutcome={ok:boolean;message:string;code?:string;retainKey:boolean;data?:unknown};
export async function runInventoryCommand(fetcher:typeof fetch,url:string,body:Record<string,unknown>,idempotencyKey:string):Promise<CommandOutcome>{try{const response=await fetcher(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...body,idempotencyKey})});let result:any;try{result=await response.json()}catch{return{ok:false,message:"The server response could not be read. Retry safely.",retainKey:true}}if(response.ok)return{ok:true,message:"Command completed.",retainKey:false,data:result};return{ok:false,message:typeof result?.message==="string"?result.message:"The command could not be completed.",code:result?.code,retainKey:result?.code!=="IDEMPOTENCY_CONFLICT"}}catch{return{ok:false,message:"The result is unknown. Check your connection and retry safely.",retainKey:true}}}
