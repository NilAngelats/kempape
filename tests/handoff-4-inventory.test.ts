import{describe,expect,it}from"vitest";import{EQUIPMENT_DEFINITIONS,CONSUMABLES,CHAOS_CARDS}from"@/lib/game/items";import{ECONOMY_CONFIG}from"@/lib/game/economy";import{readFileSync}from"node:fs";
const migration=readFileSync("supabase/migrations/0008_inventory_equipment_consumables.sql","utf8");
describe("Handoff 4 catalogs",()=>{it("contains the canonical 64 Equipment combinations",()=>{expect(EQUIPMENT_DEFINITIONS).toHaveLength(64);expect(new Set(EQUIPMENT_DEFINITIONS.map(d=>d.id)).size).toBe(64);for(const rarity of["common","rare","epic","legendary"])expect(EQUIPMENT_DEFINITIONS.filter(d=>d.rarity===rarity)).toHaveLength(16);for(const slot of["helmet","armor","legs","boots"])expect(EQUIPMENT_DEFINITIONS.filter(d=>d.slot===slot)).toHaveLength(16);for(const set of new Set(EQUIPMENT_DEFINITIONS.map(d=>d.setKey)))expect(EQUIPMENT_DEFINITIONS.filter(d=>d.setKey===set)).toHaveLength(4)});it("uses exact corrected Gold and Phoenix values",()=>{expect(ECONOMY_CONFIG.gold).toEqual({intervalMinutes:120,helmet:1,boots:1,legs:2,armor:3,fullSetBonus:1});const phoenix=EQUIPMENT_DEFINITIONS.filter(d=>d.setKey==="legendary_phoenix");expect(phoenix.reduce((n,d)=>n+d.effectValue,0)).toBe(9500);expect(phoenix[0].fullSetBonus).toBe(500)});it("contains seven Consumables and Chaos Cards",()=>{expect(CONSUMABLES).toHaveLength(7);expect(CHAOS_CARDS).toHaveLength(7)})});
describe("migration 0008 safety",()=>{it("forces RLS and denies browser execution",()=>{expect(migration).toContain("force row level security");expect(migration).toContain("from public,anon,authenticated")});it("enforces stack, slot, supply and provenance invariants",()=>{expect(migration).toContain("check(quantity between 0 and 10)");expect(migration).toContain("primary key(game_run_id,player_id,slot)");expect(migration).toContain("claimed_count<=configured_maximum");expect(migration).toContain("unique(game_run_id,source_type,source_id)")});it("uses fixed security-definer search paths",()=>{expect(migration.match(/security definer set search_path=public,pg_temp/g)?.length).toBeGreaterThanOrEqual(6)})});
